import React from 'react';
import styles from './Transparency.module.scss';

function Transparency() {
    return (
        <p className={`${styles.topPage}`}>Transparency</p>
    );
}

export default Transparency;