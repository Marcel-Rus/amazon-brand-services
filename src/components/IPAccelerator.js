import React from 'react';
import styles from './IPAccelerator.module.scss';

function IPAccelerator() {
    return (
        <p className={`${styles.topPage}`}>IP Accelerator</p>
    );
}

export default IPAccelerator;