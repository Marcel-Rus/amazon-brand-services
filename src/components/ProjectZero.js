import React from 'react';
import styles from './ProjectZero.module.scss';

function ProjectZero() {
    return (
        <p className={`${styles.topPage}`}>Project Zero</p>
    );
}

export default ProjectZero;